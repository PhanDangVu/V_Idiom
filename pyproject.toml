[project]
name = "onto-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "PhanDangVu" }
]
requires-python = ">=3.12"
dependencies = [
    "deep-translator>=1.11.4",
    "fastapi>=0.141.1",
    "ipywidgets>=8.1.9",
    "owlready2>=0.51",
    "pandas>=3.0.5",
    "uvicorn>=0.53.0",
]

[project.scripts]
onto-python = "onto_python:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
